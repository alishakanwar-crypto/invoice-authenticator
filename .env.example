APP_SECRET=replace-with-a-long-random-value
PUBLIC_BASE_URL=http://localhost:8000
MAX_UPLOAD_MB=20

# Optional SMTP. Without these, reports are rendered but not sent.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Invoice Integrity
SMTP_USE_TLS=true
